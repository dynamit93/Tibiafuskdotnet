<Window x:Class="Tibiafuskdotnet.MVVM.Views.RuneMakerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Tibiafuskdotnet.MVVM.ViewModel"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tibiafuskdotnet.MVVM.Views"
        mc:Ignorable="d"
        Title="RuneMakerView" Height="180" Width="220">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="198"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="Mana" Margin="4,36,152,86" />
        <TextBox x:Name="RuneMakerMana" Margin="51,40,2,90" Text="{Binding RuneMakerManaverb, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Spell" Margin="6,62,152,59" />
        <TextBox x:Name="RuneMakerSpell" Margin="51,65,3,65" Text="{Binding RuneMakerSpell, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Activate" Margin="3,85,120,31" />
        <CheckBox Margin="163,92,9,31" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" />

    </Grid>
</Window>
